#include "Window.h"
#include "F:/Icarus/libs/glm/glm.hpp"

int main() {
    // Create the window object
    Window myWindow;

    // Start the game loop (handle window events and user input)
    myWindow.runGameLoop();

    return 0;
}
